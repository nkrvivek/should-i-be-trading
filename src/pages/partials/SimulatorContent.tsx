/**
 * Strategy Simulator content — wraps the existing StrategiesPage content.
 * Used as a sub-tab within SignalsPage.
 */

// Re-export the existing StrategiesPage component which already has no TerminalShell
export { default } from "../StrategiesPage";
